# Overrides only — SOCKET_SERVER_SECRET lives in project root .env
APP_ENV=local
SOCKET_PORT=6001
SOCKET_HOST=127.0.0.1
APP_URL=http://127.0.0.1:8000
SOCKET_SERVER_URL=http://127.0.0.1:6001

# Live: add SSL paths + APP_ENV=production (see comments in root .env.example)
